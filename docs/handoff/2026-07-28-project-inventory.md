# 2026-07-28 项目进度与资产盘点

## 1. 当前结论

项目已经完成 Wave 0 玩法验证和 Wave 0.5 集成基线，可以换设备继续开发。
当前唯一活动基线是 `integration/wave05-qingmao`，远端仓库为
`https://github.com/jzjm042487-alt/dream-creater.git`。

本次审核前的 Wave 0.5 基线提交为 `04a2eb5`。本交接文档所在提交及标签
`wave0.5-handoff-20260728` 是换机恢复点。

审核时所有工作树均无未提交文件，stash 为空。唯一只存在本机的提交是
`codex/tactical-combat-wave1` 上的 15 个战斗 AI 提交，现已完整推送到同名远端分支。

## 2. 分支与工作状态

| 分支 | 审核时 HEAD | 远端状态 | 用途 |
| --- | --- | --- | --- |
| `integration/wave05-qingmao` | `04a2eb5` 加本交接提交 | 已跟踪远端 | 当前活动基线 |
| `codex/tactical-combat-wave1` | `2eebb0f` | 已推送 | 尚未集成的 Wave 1 战斗实现 |
| `snapshot/pre-wave05-20260727` | `a7efb44` | 已推送 | Wave 0.5 前完整快照 |
| `agent/systems-demo-v2` | `e04c0e8` | 已推送 | 系统角色历史交付 |
| `main` | `3dd8960` | 已推送 | 旧默认分支，不作为继续开发入口 |

战斗分支以 `e04c0e8` 为共同基点。相对当前集成分支，集成侧有 22 个独有提交，
战斗侧有 15 个独有提交。战斗分支仍保留旧行动点、旧方源警觉和旧偷盗结果语义，
并同时修改状态迁移、契约验证器、`GameStateStore`、战斗场景和依赖。

因此不能整分支直接合并。正确做法是从 `integration/wave05-qingmao` 新建集成分支，
按“战斗目录与数据契约、状态持久化、场景接入、测试”分批移植，并让 Wave 0.5
的精简系统和分类关系保持最高优先级。

## 3. 已完成进度

### 剧情与任务

- 青茅山状态化剧情生产包共 10 个文件、144 条唯一记录。
- 已覆盖主线、Q01 酒虫、Q02 花酒传承、Q03 贾金生、Q04 九叶生机草、
  Q05 青书命运及主要 NPC 状态对白。
- Q01 酒虫拥有成功、失去、拒绝、错过和不断主线的失败反馈。
- Phaser 活动入口已经能端到端完成 Q01 路线 A，并继续隐藏、炼化酒虫。
- 其余 131 条非 Q01 记录仍是生产级编剧输入，尚未全部编译为运行时结构化数据。

### 核心系统

- 一次选择、一次判定的偷盗公式已经在 Phaser 与 UI 原型间共享。
- 偷盗随机种子与游标可存档，关闭再打开不能重掷；普通目标每日只结算一次。
- 方源使用普通 NPC 的分类关系、已知事实和直接冲突，不存在全局数值警觉。
- 隐藏路线使用结构化节点与能力阈值，20 步只保底普通目的地。
- 战斗、低阶蛊、物品、关系图、状态迁移和原著事实层均已有设计或机器契约。

### 代码与交互

- `/` 是 Phaser 可行走 Q01 纵向切片。
- `/ui.html` 是 16 视图、5 分组的完整 UI 与旅行原型。
- 6 个运行时语义面板已经映射到原型视图，任务和存档不写入 `UIxx` 编号。
- 当前 UI 已删除行动点、功绩和方源数值警觉展示。
- 活动入口与最早的 `src/app.js` 文本验证器已经明确隔离。

### 原著事实层

- 已提交 1915 个章节定位、88 个已核验蛊名和 2 个策划精选事实文件。
- 生成索引只保留章节与原文行定位，不包含小说段落和章节标题。
- 输入原文是不完整的 GBK 文本，缺失范围已记录在
  `systems/canon/generated/source-manifest.json` 与提取报告中。

### 美术与视觉

机器清单见 `2026-07-28-asset-manifest.json`。清单覆盖：

- `public/assets/`：34 个运行时文件，包含 6 张场景、角色小图、玩家行走图和人物立绘。
- `docs/game-design/assets/`：44 个生产源图、导出图和审阅图。
- `assets/`：3 个地图或概念参考文件。
- `docs/archive/brainstorming/`：从本机忽略目录中抢救的 16 个设计可视稿、画风原图与说明。
- `docs/handoff/captures/`：7 张 Wave 0 和 Wave 0.5 阶段截图。

总计 104 个已盘点资产文件，131,798,148 字节。其中有 87 个 PNG、1 个 SVG、
13 个 HTML 可视稿和 3 个资产说明文件。

## 4. 独立战斗分支

`codex/tactical-combat-wave1` 已包含：

- 规范化战斗动作、AI 档案、遭遇和难度矩阵。
- 确定性随机、网格寻路、合法行动枚举、纯结算、效用评估和有界搜索。
- 多敌人行动顺序、存档恢复、难度冻结、浏览器战斗接入和基准报告。
- 15 个有序提交、74 个变更文件、约 13,494 行新增实现与测试。

本次新鲜验证结果：

- `131/131` 单元与属性测试通过。
- 13 个遭遇的标准难度平衡校验通过，无未决对局。
- 生产构建通过，仅有现存的大包体警告。
- Chromium 端到端测试 `14/14` 通过。

这些结果证明分支资料完整，但不代表已经满足 Wave 0.5 契约。移植时必须重新跑
Wave 0.5 的删除系统检查、偷盗单次判定检查和方源分类关系检查。

## 5. 当前技术债与风险

1. 两个 NPC 专用地图动作图测试仍明确跳过，属于 Wave 1 美术债务。
2. Q02 至 Q05 尚未转成结构化运行时数据，不能同时并行铺开。
3. 战斗分支与集成分支存在高冲突面，必须分批移植。
4. Vite 构建成功但主游戏与 UI chunk 超过 500 kB，后续需要按场景或面板拆包。
5. 仓库没有 GitHub Actions，所有门禁仍需在本地执行。
6. GitHub 默认 `main` 仍是旧基线，新设备克隆后必须主动切到活动分支。
7. 原著输入文件不进 Git，重新抽取索引时需要单独准备匹配指纹的原文。
8. 两张未选画风图和历史可视稿已归档，但生成模型、完整提示词和授权元数据未保留，
   只能作为内部原型参考。

## 6. 建议的下一步

换机后先完成 `2026-07-28-device-migration.md` 的全套验证，再进入 Wave 1。
Wave 1 的第一条生产路径仍应是 Q01 酒虫结构化纵向切片。战斗分支作为第二条受控移植线，
先接一场 Q01 相关遭遇，不要同时数据化 Q02 至 Q05。
